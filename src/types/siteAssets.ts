export type SiteAssets = {
  home: {
    heroBackgroundUrl: string;
  };

  shared: {
    pagePatternUrl: string;
  };

  auth: {
    backgroundUrl: string;
  };

  about: {
    bottleImageUrl: string;
  };

  reviews: {
    wineBackdropUrl: string;
  };

  history: {
    videoUrl: string;
  };
};